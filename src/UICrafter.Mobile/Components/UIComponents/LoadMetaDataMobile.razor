@using UICrafter.Core.Components.BaseComponents

@inherits LoadMetaDataList
@inject NavigationManager NavigationManager

@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override void HandleClick(AppView view) => NavigationManager.NavigateTo("/appview/" + view.Id);
}

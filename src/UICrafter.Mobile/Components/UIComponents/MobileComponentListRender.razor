@if (componentList != null && componentList.UIComponents.Any())
{
    <CascadingValue Value="jsonPublisher">
        <CascadingValue Value="inputFieldPublisher">
            <ComponentListRender UIComponents="componentList.UIComponents" />
        </CascadingValue>
    </CascadingValue>
}
else
{
    <MudText Typo="Typo.body1">
        No components to display.
    </MudText>
}

@code {
    [Parameter]
    public required UIComponentList componentList { get; set; }

    [Parameter]
    public required JsonPublisher jsonPublisher { get; set; }

    [Parameter]
    public required InputFieldPublisher inputFieldPublisher { get; set; }

}
